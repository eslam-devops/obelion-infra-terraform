# AWS Provider Configuration
# تكوين مزود AWS - يحدد المنطقة والنسخة

provider "aws" {
  region = var.aws_region
}

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
  required_version = ">= 1.2.0"
}
